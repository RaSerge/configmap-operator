apiVersion: apps/v1
kind: Deployment
metadata:
  name: configmapmake-controller
  namespace: kube-system
  labels:
    control-plane: configmapmake-controller
spec:
  selector:
    matchLabels:
      control-plane: configmapmake-controller
  template:
    metadata:
      labels:
        control-plane: configmapmake-controller
    spec:
      containers:
        - name: controller
          image: prod-docker-repo.scartel.dc/configmapmake-controller:v0.3.0
          imagePullPolicy: Always
          command:
            - /configmapmake-controller
            - --health-addr=:9090
            - --metrics-addr=:9091
            - --enable-leader-election
          ports:
            - name: http-health
              containerPort: 9090
            - name: http-metrics
              containerPort: 9091
          livenessProbe:
            httpGet:
              path: /healthz
              port: http-health
          resources:
            limits:
              cpu: 100m
              memory: 50Mi
            requests:
              cpu: 100m
              memory: 50Mi
      securityContext:
        runAsNonRoot: true
        runAsUser: 65534
      serviceAccountName: configmapmake-controller
